job_queue: joilet
provision_data:
  distro: focal
test_data:
  test_cmds: |
    ssh -o StrictHostKeyChecking=no ubuntu@$DEVICE_IP ifconfig
# logstash_host: 10.172.10.14 # port 5959
# agent_name: joilet